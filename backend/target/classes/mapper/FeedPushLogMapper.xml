<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quxiangshe.backend.mapper.FeedPushLogMapper">
    
    <!-- 批量插入推送日志 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO feed_push_log (note_id, author_id, target_user_id, push_mode, push_status, created_at)
        VALUES
        <foreach collection="logs" item="item" separator=",">
            (#{item.noteId}, #{item.authorId}, #{item.targetUserId}, #{item.pushMode}, #{item.pushStatus}, NOW())
        </foreach>
    </insert>
    
</mapper>